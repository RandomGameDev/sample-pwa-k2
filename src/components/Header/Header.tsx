import { cn } from "@cloudeats/robin-components";

export const Header = () => {
  return (
    <nav
      className={cn(
        "bg-primary-500 h-[46px] pt-1 text-center text-3xl font-bold"
      )}
    >
      cloudsters
    </nav>
  );
};
